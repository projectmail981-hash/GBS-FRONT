import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Garage.app',
  appName: 'Garage Billing',
  webDir: 'dist/gbs-front/browser',
  
};

export default config;
